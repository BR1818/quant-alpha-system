# 阶段二：多因子选股模型框架

> 目标：将阶段一验证有效的单因子组合成可交易的选股策略
> 预计耗时：2-3 周
> 前置条件：完成阶段一的经典因子复现

---

## 1. 多因子模型全景

```
┌──────────────────────────────────────────────────────────────────────┐
│                        多因子模型工作流                               │
│                                                                      │
│  ① 因子池        ② 预处理         ③ 因子合成        ④ 组合构建      │
│  ┌─────────┐    ┌─────────┐      ┌─────────┐      ┌─────────┐      │
│  │市值因子  │───►│去极值   │─────►│等权打分  │─────►│Top-50   │      │
│  │价值因子  │    │标准化   │      │IC加权   │      │选股     │      │
│  │动量因子  │    │行业中性化│      │最优化   │      │行业约束  │      │
│  │换手率因子│    │市值中性化│      │ML模型   │      │权重优化  │      │
│  │质量因子  │    │缺失值处理│      │         │      │         │      │
│  │波动率因子│    └─────────┘      └─────────┘      └─────────┘      │
│  └─────────┘                                            │            │
│                                                         ▼            │
│  ⑦ 迭代优化     ⑥ 绩效归因        ⑤ 回测引擎                       │
│  ┌─────────┐    ┌─────────┐      ┌─────────┐                        │
│  │参数调整  │◄───│Brinson  │◄─────│交易成本  │                       │
│  │因子增删  │    │因子归因  │      │涨跌停约束│                       │
│  │权重优化  │    │行业归因  │      │停牌处理  │                       │
│  └─────────┘    └─────────┘      │月度调仓  │                       │
│                                   └─────────┘                        │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 2. 因子预处理（详细版）

### 2.1 截面预处理流程

每个交易日（或调仓日）的因子值预处理流程：

```python
def cross_sectional_preprocess(df, factor_col, industry_col, 
                                mv_col="total_mv", method="mad"):
    """
    截面因子预处理完整流程
    
    步骤：
    1. 缺失值处理
    2. 去极值
    3. 行业中性化
    4. 市值中性化（可选）
    5. 标准化
    """
    result = df.copy()
    
    # Step 1: 缺失值处理
    # 方案 A：行业中位数填充（推荐）
    result[factor_col] = result.groupby(industry_col)[factor_col].transform(
        lambda x: x.fillna(x.median())
    )
    # 方案 B：直接丢弃（更保守）
    # result = result.dropna(subset=[factor_col])
    
    # Step 2: 去极值（MAD 方法）
    median_val = result[factor_col].median()
    mad_val = (result[factor_col] - median_val).abs().median()
    dm = 1.4826 * mad_val  # MAD 到标准差的换算
    result[factor_col] = result[factor_col].clip(
        median_val - 5 * dm, median_val + 5 * dm
    )
    
    # Step 3: 行业中性化（WLS 回归法）
    # y = factor, X = 行业哑变量
    from sklearn.linear_model import LinearRegression
    industry_dummies = pd.get_dummies(result[industry_col], drop_first=True)
    model = LinearRegression().fit(industry_dummies, result[factor_col])
    result[factor_col] = result[factor_col] - model.predict(industry_dummies)
    
    # Step 4: 市值中性化（可选，对小盘因子很重要）
    log_mv = np.log(result[mv_col])
    from numpy.polynomial import polynomial as P
    coef = np.polyfit(log_mv, result[factor_col], 1)
    result[factor_col] = result[factor_col] - np.polyval(coef, log_mv)
    
    # Step 5: Z-Score 标准化
    mean_val = result[factor_col].mean()
    std_val = result[factor_col].std()
    if std_val > 1e-8:
        result[factor_col] = (result[factor_col] - mean_val) / std_val
    
    return result
```

### 2.2 特殊因子的预处理注意事项

| 因子类型 | 特殊处理 | 原因 |
|---------|---------|------|
| 财务因子（ROE等） | 季度更新，中间交易日使用最近一期值 | 财报不是每天更新的 |
| 估值因子（PE/PB） | 剔除负值或亏损股单独分组 | 亏损股 PE 无意义 |
| 波动率因子 | 要求窗口期内交易日 ≥ 15天 | 长期停牌导致波动率失真 |
| 换手率因子 | 剔除上市不足 60 天的次新股 | 次新股换手率异常高 |
| 行业因子 | 用 PIT 行业分类 | 有些股票会变更行业 |

---

## 3. 因子合成方法

### 3.1 等权打分法（最简单，推荐起步使用）

```python
def equal_weight_score(factor_dict):
    """
    等权打分：所有因子 Z-Score 直接加总
    
    factor_dict: {"factor_name": pd.Series(factor_values)}
    """
    scores = pd.DataFrame(factor_dict)
    # 每个因子 Z-Score 标准化
    scores = scores.apply(lambda x: (x - x.mean()) / x.std())
    # 等权加总
    composite_score = scores.mean(axis=1)
    return composite_score
```

**优点**：简单、稳定、不需要样本外验证
**缺点**：假设所有因子同等重要

### 3.2 IC 加权法（考虑因子预测能力差异）

```python
def ic_weighted_score(factor_dict, ic_history, halflife=60):
    """
    IC 加权：按历史 ICIR 分配权重
    
    ic_history: 每个因子的 IC 时间序列
    halflife: 半衰期（指数衰减，近期 IC 权重更大）
    """
    scores = pd.DataFrame(factor_dict)
    scores = scores.apply(lambda x: (x - x.mean()) / x.std())
    
    # 计算每个因子的加权 ICIR
    weights = {}
    for name, ic_series in ic_history.items():
        # 指数衰减权重
        n = len(ic_series)
        decay_weights = np.exp(-np.log(2) * np.arange(n)[::-1] / halflife)
        weighted_ic = np.average(ic_series, weights=decay_weights)
        weighted_std = np.sqrt(np.average(
            (ic_series - weighted_ic)**2, weights=decay_weights
        ))
        weights[name] = weighted_ic / weighted_std if weighted_std > 0 else 0
    
    # 归一化权重
    total = sum(abs(w) for w in weights.values())
    if total > 0:
        weights = {k: v / total for k, v in weights.items()}
    
    # 加权合成
    composite = sum(scores[name] * w for name, w in weights.items())
    return composite, weights
```

### 3.3 最大化 IC_IR 法（优化方法）

```python
def max_icir_combination(factor_values_panel, forward_returns_panel):
    """
    最大化组合因子的 ICIR
    
    求解：max w'μ / sqrt(w'Σw)
    其中 μ = 各因子IC均值向量，Σ = IC协方差矩阵
    
    等价于 w* = Σ^(-1) μ（类似均值方差优化）
    """
    from numpy.linalg import inv
    
    # 计算每个因子每期的 IC
    n_factors = factor_values_panel.shape[1]
    ic_matrix = []
    
    for t in range(len(factor_values_panel)):
        ics = []
        for j in range(n_factors):
            ic, _ = stats.spearmanr(
                factor_values_panel.iloc[t, j],
                forward_returns_panel.iloc[t]
            )
            ics.append(ic)
        ic_matrix.append(ics)
    
    ic_matrix = np.array(ic_matrix)
    mu = ic_matrix.mean(axis=0)       # IC 均值向量
    sigma = np.cov(ic_matrix.T)       # IC 协方差矩阵
    
    # 求解最优权重（加正则化防止过拟合）
    sigma_reg = sigma + 0.1 * np.eye(n_factors)  # 岭回归正则化
    w_star = inv(sigma_reg) @ mu
    w_star = w_star / np.sum(np.abs(w_star))  # 归一化
    
    return w_star
```

### 3.4 方法选择建议

| 方法 | 因子数量 | 数据要求 | 过拟合风险 | 推荐场景 |
|------|---------|---------|-----------|---------|
| 等权打分 | 3-10 | 低 | 最低 | 起步阶段、因子质量相近 |
| IC 加权 | 3-20 | 中 | 低 | 因子 IC 差异大、需要动态调整 |
| 最大化 ICIR | 5-30 | 高 | 中 | 因子库成熟、有足够样本外数据 |
| ML 元模型 | 10+ | 很高 | 高 | 因子很多、非线性交互强 |

---

## 4. 组合构建

### 4.1 选股规则

```python
class StockSelector:
    """选股器"""
    
    def __init__(self, top_n=50, min_mv=None, max_mv=None,
                 exclude_st=True, exclude_new_days=60):
        self.top_n = top_n
        self.min_mv = min_mv
        self.max_mv = max_mv
        self.exclude_st = exclude_st
        self.exclude_new_days = exclude_new_days
    
    def select(self, scores, stock_info, trade_date):
        """
        从综合打分中选出 Top-N 股票
        
        scores: Series(index=ts_code, values=composite_score)
        stock_info: DataFrame 含 list_date, list_status 等
        """
        valid = scores.copy()
        
        # 1. 剔除 ST
        if self.exclude_st:
            st_list = stock_info[stock_info["name"].str.contains("ST")].index
            valid = valid.drop(st_list, errors="ignore")
        
        # 2. 剔除次新股
        if self.exclude_new_days:
            new_stocks = stock_info[
                (pd.to_datetime(trade_date) - 
                 pd.to_datetime(stock_info["list_date"])).dt.days 
                < self.exclude_new_days
            ].index
            valid = valid.drop(new_stocks, errors="ignore")
        
        # 3. 市值过滤
        if self.min_mv:
            small = stock_info[stock_info["total_mv"] < self.min_mv].index
            valid = valid.drop(small, errors="ignore")
        
        # 4. 涨停不可买入
        # limit_up_list = ...
        # valid = valid.drop(limit_up_list, errors="ignore")
        
        # 5. 选 Top-N
        selected = valid.nlargest(self.top_n)
        return selected
```

### 4.2 权重分配

| 权重方案 | 计算方式 | 优点 | 缺点 |
|---------|---------|------|------|
| **等权** | 1/N | 最简单、最分散 | 小盘股实际投入多 |
| **市值加权** | w_i = mv_i / Σmv | 容量大、冲击小 | 偏向大盘股 |
| **打分加权** | w_i = score_i / Σscore | 高分股多配 | 集中度可能高 |
| **风险平价** | w_i ∝ 1/σ_i | 风险均衡 | 需要估计波动率 |
| **最大分散化** | max Σw_iσ_i / σ_p | 最大化分散 | 计算复杂 |

### 4.3 行业约束

```python
def apply_industry_constraints(weights, industry_map, 
                                max_industry_weight=0.15,
                                benchmark_weights=None):
    """
    行业约束：单行业权重不超过阈值（或相对基准偏离不超过阈值）
    
    weights: Series(index=ts_code, values=weight)
    industry_map: dict(ts_code -> industry_name)
    """
    # 计算行业暴露
    w = weights.copy()
    industries = pd.Series(industry_map)
    industry_weights = w.groupby(industries).sum()
    
    # 超配行业等比例缩减
    for ind, ind_w in industry_weights.items():
        if ind_w > max_industry_weight:
            mask = industries == ind
            stocks_in_ind = w[mask]
            scale = max_industry_weight / ind_w
            w[mask] = stocks_in_ind * scale
    
    # 重新归一化
    w = w / w.sum()
    return w
```

---

## 5. 回测框架

### 5.1 回测参数标准

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| 回测区间 | 2015-01-01 ~ 2025-12-31 | 覆盖至少一轮完整牛熊 |
| 样本内 | 2015-2023（80%） | 用于因子筛选和参数调优 |
| 样本外 | 2024-2025（20%） | 纯净评估，不可回看 |
| 调仓频率 | 月度（每月第一个交易日） | 兼顾因子衰减和交易成本 |
| 交易成本 | 单边 0.15%（含印花税+佣金+冲击） | 保守估计 |
| 基准指数 | 沪深300（000300.SH） | 大盘基准 |
| 选股数量 | 50 只 | 足够分散，容量可行 |
| 权重方式 | 等权 | 起步阶段最简单 |

### 5.2 回测核心逻辑

```python
class SimpleBacktester:
    """简易回测引擎"""
    
    def __init__(self, cost=0.0015, benchmark="000300.SH"):
        self.cost = cost
        self.benchmark = benchmark
    
    def run(self, signal_df, price_df, rebalance_dates):
        """
        signal_df: DataFrame(index=rebalance_date, columns=ts_code, values=weight)
        price_df: DataFrame(index=trade_date, columns=ts_code, values=close)
        rebalance_dates: List[str] 调仓日列表
        
        返回：组合净值序列、各期持仓明细
        """
        portfolio_nav = [1.0]
        holdings = pd.Series(dtype=float)  # 当前持仓权重
        
        all_dates = price_df.index.sort_values()
        
        for i, date in enumerate(all_dates[1:], 1):
            prev_date = all_dates[i - 1]
            
            # 计算当日收益
            if len(holdings) > 0:
                daily_returns = (
                    price_df.loc[date, holdings.index] / 
                    price_df.loc[prev_date, holdings.index] - 1
                ).fillna(0)
                port_return = (holdings * daily_returns).sum()
            else:
                port_return = 0
            
            # 是否调仓
            if date in rebalance_dates and date in signal_df.index:
                new_holdings = signal_df.loc[date].dropna()
                new_holdings = new_holdings[new_holdings > 0]
                new_holdings = new_holdings / new_holdings.sum()
                
                # 计算换手率（交易成本）
                if len(holdings) > 0:
                    turnover = (new_holdings - holdings).abs().sum() / 2
                else:
                    turnover = 1.0  # 首次建仓
                
                # 扣除交易成本
                port_return -= turnover * self.cost
                
                holdings = new_holdings
            
            portfolio_nav.append(portfolio_nav[-1] * (1 + port_return))
        
        return pd.Series(portfolio_nav, index=all_dates)
```

### 5.3 绩效指标

```python
def performance_metrics(nav_series, benchmark_series=None, rf=0.02):
    """
    计算全套绩效指标
    
    nav_series: 组合净值序列（日频）
    benchmark_series: 基准净值序列
    rf: 无风险利率（年化）
    """
    returns = nav_series.pct_change().dropna()
    n_years = len(returns) / 252
    
    metrics = {}
    
    # 收益指标
    metrics["总收益"] = nav_series.iloc[-1] / nav_series.iloc[0] - 1
    metrics["年化收益"] = (1 + metrics["总收益"]) ** (1/n_years) - 1
    metrics["年化波动"] = returns.std() * np.sqrt(252)
    metrics["夏普比率"] = (metrics["年化收益"] - rf) / metrics["年化波动"]
    
    # 回撤指标
    cummax = nav_series.cummax()
    drawdown = (nav_series - cummax) / cummax
    metrics["最大回撤"] = drawdown.min()
    metrics["最大回撤持续天数"] = _max_drawdown_duration(drawdown)
    metrics["卡玛比率"] = metrics["年化收益"] / abs(metrics["最大回撤"])
    
    # 收益分布
    metrics["偏度"] = returns.skew()
    metrics["峰度"] = returns.kurtosis()
    metrics["日胜率"] = (returns > 0).mean()
    metrics["盈亏比"] = returns[returns > 0].mean() / abs(returns[returns < 0].mean())
    
    # 相对指标（需要基准）
    if benchmark_series is not None:
        bm_returns = benchmark_series.pct_change().dropna()
        excess_returns = returns - bm_returns
        metrics["超额年化收益"] = excess_returns.mean() * 252
        metrics["跟踪误差"] = excess_returns.std() * np.sqrt(252)
        metrics["信息比率"] = metrics["超额年化收益"] / metrics["跟踪误差"]
        
        # Alpha & Beta
        from scipy.stats import linregress
        slope, intercept, _, _, _ = linregress(bm_returns, returns)
        metrics["Beta"] = slope
        metrics["Alpha(年化)"] = intercept * 252
    
    return metrics
```

---

## 6. 绩效归因

### 6.1 Brinson 行业归因

```python
def brinson_attribution(port_weights, port_returns, 
                         bm_weights, bm_returns, industries):
    """
    Brinson 行业归因：分解超额收益为 配置效应 + 选股效应 + 交互效应
    
    配置效应 = Σ(wp_i - wb_i) × rb_i     → 行业配置贡献
    选股效应 = Σ wb_i × (rp_i - rb_i)     → 行业内选股贡献
    交互效应 = Σ(wp_i - wb_i) × (rp_i - rb_i) → 交互项
    """
    result = {}
    
    for ind in industries.unique():
        wp = port_weights[industries == ind].sum()  # 组合行业权重
        wb = bm_weights[industries == ind].sum()    # 基准行业权重
        
        rp = (port_weights[industries == ind] * 
              port_returns[industries == ind]).sum() / wp if wp > 0 else 0
        rb = (bm_weights[industries == ind] * 
              bm_returns[industries == ind]).sum() / wb if wb > 0 else 0
        
        result[ind] = {
            "配置效应": (wp - wb) * rb,
            "选股效应": wb * (rp - rb),
            "交互效应": (wp - wb) * (rp - rb),
        }
    
    return pd.DataFrame(result).T
```

### 6.2 因子收益归因

```python
def factor_return_attribution(port_returns, factor_exposures):
    """
    因子收益归因：将组合收益分解为各因子的贡献
    
    r_p = Σ β_i × f_i + ε
    
    其中 β_i 是组合在因子 i 上的暴露，f_i 是因子收益率
    """
    from sklearn.linear_model import LinearRegression
    
    model = LinearRegression().fit(factor_exposures, port_returns)
    
    attribution = {}
    for i, factor_name in enumerate(factor_exposures.columns):
        attribution[factor_name] = {
            "因子暴露(β)": model.coef_[i],
            "因子收益贡献": model.coef_[i] * factor_exposures[factor_name].mean(),
        }
    attribution["残差(Alpha)"] = {
        "因子暴露(β)": None,
        "因子收益贡献": model.intercept_,
    }
    
    return pd.DataFrame(attribution).T
```

---

## 7. 阶段二的成功标准

完成阶段二后，你应该：

- [ ] 构建了一个 3-5 因子的多因子模型
- [ ] 模型在样本内年化超额收益 > 5%（相对沪深300）
- [ ] 模型在样本外超额收益方向一致（不需要和样本内一样好）
- [ ] 理解了行业/市值中性化的重要性
- [ ] 能够做 Brinson 归因，知道超额来源
- [ ] 换手率控制在月度单边 30% 以内
- [ ] 最大回撤 < 25%

---

## 8. 进阶讨论：A 股多因子模型的特殊挑战

| 挑战 | 说明 | 应对策略 |
|------|------|---------|
| **因子拥挤** | 经典因子被广泛使用，Alpha 衰减 | 关注因子创新（非线性组合、另类数据） |
| **风格轮动** | A 股风格切换剧烈（大小盘轮动周期 2-3 年） | 动态权重 or 风格中性 |
| **政策冲击** | 监管政策突变（如量化限制、印花税调整） | 分散化 + 尾部风险对冲 |
| **流动性约束** | 选出的小盘股可能无法买入足够仓位 | 设置最低流动性阈值 |
| **散户主导** | 散户交易占比高，行为偏差显著 | 利用散户行为偏差（如追涨杀跌、处置效应） |

---

> 完成本阶段后，进入 **03-机器学习因子挖掘.md**
