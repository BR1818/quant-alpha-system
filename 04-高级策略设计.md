# 阶段四：高级策略设计

> 目标：将因子和模型组合成完整的可交易策略，覆盖多种策略类型
> 预计耗时：3-4 周
> 前置条件：完成阶段二（多因子框架）和阶段三（ML 模型）

---

## 1. 策略类型总览

基于 QuantDB 的数据资产，可以构建以下五大类策略：

```
┌─────────────────────────────────────────────────────────────────┐
│                        策略金字塔                                │
│                                                                  │
│                    ┌──────────────┐                              │
│                    │  组合优化    │  ← 最高级：多策略组合         │
│                    └──────┬───────┘                              │
│               ┌──────────┴──────────┐                           │
│               │    高级策略          │                           │
│    ┌──────────┴──────────┬──────────┴──────────┐                │
│    │ 指数增强 │ 行业轮动  │ 事件驱动  │ 概念挖掘 │                │
│    └──────────┴──────────┴──────────┴──────────┘                │
│    ┌──────────────────────────────────────────────┐             │
│    │              基础策略                          │             │
│    │     多因子选股    │    择时策略                  │             │
│    └──────────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 策略一：指数增强（Index Enhancement）

### 2.1 核心思路

> 跟踪基准指数（如沪深300），在基准之上获取超额收益（Alpha）。
> 约束条件：跟踪误差控制在 3-5% 以内。

```
基准指数（沪深300）             增强组合
成分股 A  权重 3%    ──►    A  权重 4%   （超配，因子看好）
成分股 B  权重 2%    ──►    B  权重 1%   （低配，因子看空）
成分股 C  权重 1%    ──►    C  权重 1.5% （小幅超配）
非成分股 D 权重 0%    ──►    D  权重 0.5% （少量配置，风险可控）
```

### 2.2 实现框架

```python
class IndexEnhancer:
    """指数增强策略"""
    
    def __init__(self, 
                 benchmark="000300.SH",  # 沪深300
                 max_tracking_error=0.05, # 最大跟踪误差
                 max_stock_deviation=0.02, # 单股最大偏离
                 max_industry_deviation=0.05): # 行业最大偏离
        self.benchmark = benchmark
        self.max_te = max_tracking_error
        self.max_stock_dev = max_stock_deviation
        self.max_ind_dev = max_industry_deviation
    
    def optimize(self, alpha_scores, bm_weights, cov_matrix, 
                  industry_map):
        """
        组合优化：最大化预期超额收益，约束跟踪误差
        
        min  -w'α + λ(w-w_bm)'Σ(w-w_bm)  
        s.t. |w_i - w_bm_i| ≤ 0.02         # 单股偏离约束
             |Σ_{i∈ind}w_i - Σ_{i∈ind}w_bm_i| ≤ 0.05  # 行业偏离
             Σw_i = 1                        # 权重和为1
             w_i ≥ 0                         # 禁止做空
        """
        from scipy.optimize import minimize
        
        n = len(alpha_scores)
        w_bm = bm_weights.values
        alpha = alpha_scores.values
        
        # 目标函数：最大化Alpha - 跟踪误差惩罚
        def objective(w):
            excess = w - w_bm
            te_squared = excess @ cov_matrix @ excess
            return -(w @ alpha) + 50 * te_squared  # λ=50
        
        # 约束
        constraints = [
            {"type": "eq", "fun": lambda w: w.sum() - 1},  # 权重和=1
        ]
        
        # 边界
        bounds = [(max(0, w_bm[i] - self.max_stock_dev),
                    min(1, w_bm[i] + self.max_stock_dev)) 
                   for i in range(n)]
        
        result = minimize(objective, w_bm, bounds=bounds, 
                          constraints=constraints, method="SLSQP")
        
        return pd.Series(result.x, index=alpha_scores.index)
```

### 2.3 指数增强的数据需求

| 数据 | 来源表 | 用途 |
|------|--------|------|
| 成分股权重 | index_weight | 基准组合 |
| 行业分类 | stock_industry | 行业约束 |
| 日线行情 | daily | 协方差矩阵估计 |
| 因子打分 | 阶段二/三的因子模型 | Alpha 信号 |
| 涨跌停 | stk_limit | 交易约束 |

### 2.4 评估指标

| 指标 | 目标 | 说明 |
|------|------|------|
| 超额年化收益 | > 8% | 相对基准的超额 |
| 信息比率 IR | > 1.5 | 超额收益/跟踪误差 |
| 跟踪误差 | < 5% | 和基准的偏离程度 |
| 超额最大回撤 | < 8% | 超额收益的回撤 |
| 月度胜率 | > 60% | 每月跑赢基准的概率 |

---

## 3. 策略二：行业轮动（Sector Rotation）

### 3.1 核心思路

> 不选个股，而是在 31 个申万一级行业之间做配置，超配看好的行业，低配看空的行业。

```
行业轮动信号：
  电子 ↑↑  →  超配 15%
  银行 ↓   →  低配 2%
  医药 ↑   →  超配 10%
  钢铁 ↓↓  →  空仓 0%
  ...
```

### 3.2 行业轮动因子

```python
# ==================== 行业动量 ====================
# 数据来源：ths_daily / dc_daily / 或自行用 stock_industry + daily 计算

def calc_industry_momentum(industry_returns, window=20):
    """行业N日动量"""
    return industry_returns.rolling(window).sum()

# ==================== 行业估值 ====================
# 行业PE中位数 / 行业PB中位数（横截面排名）
def calc_industry_valuation(stock_pe, industry_map):
    """行业估值（PE中位数）"""
    return stock_pe.groupby(industry_map).median()

# ==================== 行业资金流 ====================
# 北向资金分行业净流入
def calc_industry_north_flow(moneyflow, industry_map):
    """行业北向资金净流入"""
    return moneyflow.groupby(industry_map)["net_mf_amount"].sum()

# ==================== 行业拥挤度 ====================
# 换手率偏离历史均值的程度
def calc_industry_crowding(turnover, industry_map, window=60):
    """行业拥挤度 = 当前换手率 / 历史均值"""
    ind_turnover = turnover.groupby(industry_map).mean()
    return ind_turnover / ind_turnover.rolling(window).mean()

# ==================== 概念热度 ====================
# 利用 dc_index 的板块涨跌幅和成交额
def calc_concept_heat(dc_daily, threshold=3.0):
    """
    概念热度：涨幅>3%的概念板块数量 
    及其与行业的映射关系
    """
    hot_concepts = dc_daily[dc_daily["pct_change"] > threshold]
    return hot_concepts.groupby("trade_date").size()
```

### 3.3 行业轮动模型

| 方法 | 描述 | 适用场景 |
|------|------|---------|
| **动量排序** | 选过去20日涨幅最大的N个行业 | 趋势行情 |
| **估值+动量** | 低估值且有动量的行业 | 均衡配置 |
| **宏观映射** | 经济周期 → 行业映射（美林时钟） | 长周期配置 |
| **ML 预测** | 用 LightGBM 预测行业未来收益 | 数据驱动 |

---

## 4. 策略三：事件驱动（Event-Driven）

### 4.1 可挖掘的事件类型

基于 QuantDB 的数据，可以研究以下事件的收益效应：

| 事件 | 数据来源 | 信号逻辑 | 预期效应 |
|------|---------|---------|---------|
| **业绩预增** | forecast.type = "预增" | 公告日后买入 | 正向（+3~8%，20日内） |
| **业绩预减** | forecast.type = "预减" | 公告日后卖出 | 负向 |
| **限售股解禁** | share_float | 解禁日前10日卖出 | 负向（抛压预期） |
| **龙虎榜机构买入** | top_list.buy_amount | 机构净买入时跟进 | 正向（短期2-5日） |
| **涨停首板** | limit_list_d.up_stat="1/1" | 首板次日开盘买入 | 高波动（需择时） |
| **北向大幅流入** | moneyflow_hsgt.north_money > 100亿 | 大幅流入后买入 | 正向（滞后1-3日） |
| **高送转预期** | dividend.div_total > 0.5 | 预案公告后买入 | A股特色效应 |
| **融资余额突增** | margin_detail.rzye 变化率 | 杠杆资金涌入 | 短期正向 |

### 4.2 事件研究方法（Event Study）

```python
def event_study(event_dates, price_df, window=(-10, 30)):
    """
    事件研究法：计算事件前后的累计异常收益（CAR）
    
    event_dates: DataFrame(ts_code, event_date)
    price_df: 日线价格数据
    window: (pre_event_days, post_event_days)
    """
    pre, post = window
    results = []
    
    for _, row in event_dates.iterrows():
        ts_code = row["ts_code"]
        event_date = row["event_date"]
        
        # 获取事件前后的价格
        stock_prices = price_df[price_df["ts_code"] == ts_code].copy()
        stock_prices = stock_prices.sort_values("trade_date")
        
        # 找到事件日在价格序列中的位置
        event_idx = stock_prices[
            stock_prices["trade_date"] == event_date
        ].index
        
        if len(event_idx) == 0:
            continue
        event_idx = event_idx[0]
        
        # 提取窗口期收益率
        start_idx = event_idx + pre
        end_idx = event_idx + post
        
        window_prices = stock_prices.loc[start_idx:end_idx, "close"]
        if len(window_prices) < abs(pre) + post:
            continue
        
        # 计算累计收益率（以事件日前一天为基准）
        base_price = stock_prices.loc[event_idx + pre - 1, "close"]
        cum_returns = window_prices / base_price - 1
        
        results.append(cum_returns.values)
    
    # 计算平均 CAR
    results = np.array(results)
    avg_car = results.mean(axis=0)
    
    return avg_car, results

# 使用示例：业绩预增事件
forecast_df = pd.read_parquet("data/curated/forecast/...")
events = forecast_df[forecast_df["type"] == "预增"][["ts_code", "ann_date"]]
events.columns = ["ts_code", "event_date"]

avg_car, all_cars = event_study(events, daily_df, window=(-10, 30))

# 画图：事件前后的平均累计收益
import matplotlib.pyplot as plt
plt.plot(range(-10, 31), avg_car)
plt.axvline(x=0, color="r", linestyle="--", label="事件日")
plt.xlabel("相对事件日（交易日）")
plt.ylabel("累计异常收益")
plt.title("业绩预增公告后的累计收益")
```

### 4.3 业绩预告交易策略

```python
class ForecastStrategy:
    """业绩预告事件策略"""
    
    def __init__(self, hold_days=20, top_n=30):
        self.hold_days = hold_days
        self.top_n = top_n
    
    def generate_signal(self, trade_date, forecast_df, daily_df):
        """
        信号生成逻辑：
        1. 找到最近 hold_days 内公告的"预增"/"扭亏"事件
        2. 按预期净利润增长率排序
        3. 选 Top-N 买入
        """
        # 过去 hold_days 内的预增公告
        start_date = trade_calendar.shift(trade_date, -self.hold_days)
        
        recent_forecasts = forecast_df[
            (forecast_df["ann_date"] >= start_date) &
            (forecast_df["ann_date"] <= trade_date) &
            (forecast_df["type"].isin(["预增", "扭亏", "略增"]))
        ]
        
        # 按预期增长率排序（取中值）
        recent_forecasts["expected_change"] = (
            recent_forecasts["p_change_min"] + 
            recent_forecasts["p_change_max"]
        ) / 2
        
        # 选 Top-N
        top_stocks = recent_forecasts.nlargest(
            self.top_n, "expected_change"
        )["ts_code"].tolist()
        
        return top_stocks
```

---

## 5. 策略四：概念板块轮动

### 5.1 核心思路

> 利用 A 股独特的「题材炒作」文化，追踪概念板块的动量效应。

```python
class ConceptRotation:
    """概念板块轮动策略"""
    
    def __init__(self, top_n_concepts=5, top_n_stocks=50):
        self.top_n_concepts = top_n_concepts
        self.top_n_stocks = top_n_stocks
    
    def select_hot_concepts(self, dc_daily, trade_date, lookback=5):
        """
        选择热门概念板块：
        1. 过去 N 日涨幅排名前 K 的概念
        2. 且成交额/换手率 > 阈值（确认是真热点）
        """
        recent = dc_daily[
            (dc_daily["trade_date"] >= shift_date(trade_date, -lookback)) &
            (dc_daily["trade_date"] <= trade_date)
        ]
        
        # 按概念板块计算累计涨幅
        concept_momentum = recent.groupby("ts_code").agg({
            "pct_change": "sum",          # 累计涨幅
            "turnover_rate": "mean",       # 平均换手率
            "up_num": "last",              # 最新上涨家数
            "down_num": "last",            # 最新下跌家数
        })
        
        # 过滤：换手率 > 1% 且上涨家数 > 下跌家数
        concept_momentum = concept_momentum[
            (concept_momentum["turnover_rate"] > 1) &
            (concept_momentum["up_num"] > concept_momentum["down_num"])
        ]
        
        # 按涨幅排序，选 Top-K
        hot_concepts = concept_momentum.nlargest(
            self.top_n_concepts, "pct_change"
        )
        
        return hot_concepts.index.tolist()
    
    def select_stocks(self, hot_concepts, dc_member, 
                       stock_scores, trade_date):
        """
        从热门概念中选股：
        1. 提取热门概念的成分股
        2. 结合基本面因子打分
        3. 选 Top-N
        """
        # 热门概念成分股
        hot_stocks = dc_member[
            dc_member["ts_code"].isin(hot_concepts)
        ]["con_code"].unique()
        
        # 在热门成分股中，按因子打分选 Top-N
        scores = stock_scores[stock_scores.index.isin(hot_stocks)]
        selected = scores.nlargest(self.top_n_stocks)
        
        return selected.index.tolist()
```

### 5.2 概念板块研究方向

| 研究方向 | 数据需求 | 核心问题 |
|---------|---------|---------|
| 概念动量 | dc_daily / ths_daily | 涨幅前10的概念，未来5日是否继续领涨？ |
| 概念反转 | 同上 | 连跌3日以上的概念，是否会反弹？ |
| 概念扩散 | dc_member + daily | 概念板块领涨股上涨后，成分股是否跟涨？ |
| 概念新创 | dc_index.list_date | 新创建的概念板块（如新政策题材）是否有首日效应？ |
| 概念拥挤 | dc_daily.turnover_rate | 换手率最高的概念是否即将见顶？ |
| 多概念交叉 | dc_member | 同时属于多个热门概念的股票是否更强？ |

---

## 6. 策略五：北向资金跟踪

### 6.1 核心逻辑

> 北向资金（外资通过沪深港通买入 A 股）被市场称为「聪明钱」。研究表明北向资金的流向对短期和中期收益有一定的预测能力。

```python
class NorthboundStrategy:
    """北向资金跟踪策略"""
    
    def __init__(self, threshold_big_inflow=100):  # 100亿以上为大幅流入
        self.threshold = threshold_big_inflow
    
    def market_signal(self, hsgt_df, trade_date, lookback=5):
        """
        市场层面择时信号：
        - 连续 N 日北向净流入 → 看多
        - 连续 N 日北向净流出 → 看空
        - 单日大幅流入 > 100 亿 → 强烈看多
        """
        recent = hsgt_df[
            hsgt_df["trade_date"] <= trade_date
        ].tail(lookback)
        
        # 连续流入天数
        consecutive_inflow = (recent["north_money"] > 0).sum()
        # 累计流入金额
        total_inflow = recent["north_money"].sum()
        # 是否有大幅流入日
        has_big_inflow = (recent["north_money"] > self.threshold).any()
        
        if consecutive_inflow >= 4 or has_big_inflow:
            return "BULLISH"
        elif consecutive_inflow <= 1 and total_inflow < 0:
            return "BEARISH"
        else:
            return "NEUTRAL"
    
    def stock_signal(self, moneyflow_df, trade_date, top_n=50):
        """
        个股层面选股信号：
        - 主力资金（大单+超大单）净流入排名前 N
        - 结合北向资金方向
        """
        today = moneyflow_df[moneyflow_df["trade_date"] == trade_date]
        
        # 主力净流入 = (大单买入+超大单买入) - (大单卖出+超大单卖出)
        today["main_net_flow"] = (
            today["buy_lg_vol"] + today["buy_elg_vol"] - 
            today["sell_lg_vol"] - today["sell_elg_vol"]
        )
        
        # 选主力净流入 Top-N
        top_stocks = today.nlargest(top_n, "main_net_flow")
        
        return top_stocks["ts_code"].tolist()
```

---

## 7. 策略组合与风险管理

### 7.1 多策略组合

```python
class StrategyEnsemble:
    """多策略组合管理器"""
    
    def __init__(self, strategies, weights=None):
        """
        strategies: dict(name -> strategy_instance)
        weights: dict(name -> weight), 默认等权
        """
        self.strategies = strategies
        self.weights = weights or {
            name: 1/len(strategies) for name in strategies
        }
    
    def generate_combined_signal(self, trade_date, **kwargs):
        """
        合并多个策略的信号
        
        方法：将每个策略的选股打分归一化后加权合并
        """
        combined_scores = pd.Series(dtype=float)
        
        for name, strategy in self.strategies.items():
            signal = strategy.generate_signal(trade_date, **kwargs)
            # 归一化为 0-1 分数
            if isinstance(signal, pd.Series):
                signal = (signal - signal.min()) / (signal.max() - signal.min() + 1e-8)
                combined_scores = combined_scores.add(
                    signal * self.weights[name], fill_value=0
                )
        
        return combined_scores
```

### 7.2 风险管理框架

| 风险类型 | 控制措施 | 参数 |
|---------|---------|------|
| 个股集中度 | 单股最大权重 | ≤ 5% |
| 行业集中度 | 单行业最大权重 | ≤ 20% |
| 组合波动率 | 波动率目标 | 年化 ≤ 20% |
| 最大回撤 | 回撤止损 | ≤ 15% 触发减仓 |
| 换手率 | 换手率上限 | 月度单边 ≤ 40% |
| 流动性 | 最低日成交额 | ≥ 500 万元 |

```python
class RiskManager:
    """风险管理器"""
    
    def __init__(self, max_drawdown=-0.15, vol_target=0.20):
        self.max_drawdown = max_drawdown
        self.vol_target = vol_target
        self.peak_nav = 1.0
    
    def check_drawdown(self, current_nav):
        """回撤监控"""
        self.peak_nav = max(self.peak_nav, current_nav)
        drawdown = (current_nav - self.peak_nav) / self.peak_nav
        
        if drawdown < self.max_drawdown:
            return "REDUCE_POSITION"  # 触发减仓
        elif drawdown < self.max_drawdown * 0.7:
            return "WARNING"           # 预警
        else:
            return "NORMAL"
    
    def vol_scaling(self, target_weights, cov_matrix):
        """
        波动率调控：当预估波动率 > 目标时，等比例缩减仓位
        """
        w = target_weights.values
        port_vol = np.sqrt(w @ cov_matrix @ w) * np.sqrt(252)
        
        if port_vol > self.vol_target:
            scale = self.vol_target / port_vol
            return target_weights * scale
        return target_weights
```

---

## 8. 阶段四的成功标准

- [ ] 实现了至少 2 种策略类型（如指数增强 + 事件驱动）
- [ ] 每个策略有完整的回测报告（含交易成本）
- [ ] 理解了组合优化的基本方法
- [ ] 实现了基本的风险管理框架
- [ ] 策略在样本外有正超额收益

---

> 完成本阶段后，查看 **05-数据规律发现.md** 获取 A 股特有规律的灵感
