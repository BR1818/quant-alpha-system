# 阶段一：经典因子复现指南

> 目标：验证数据正确性 + 建立因子研究 Pipeline + 复现学术界/业界公认有效的 Alpha 因子
> 预计耗时：2-3 周
> 前置条件：QuantDB 数据已就绪，Python 环境已配置

---

## 为什么要从复现经典因子开始？

1. **验证数据质量**：如果经典因子在你的数据上完全失效，大概率是数据处理有问题
2. **建立 Baseline**：所有后续研究的比较基准
3. **学习 Pipeline**：因子计算 → 预处理 → IC 分析 → 分组回测 → 结论，这个流程会反复使用
4. **理解 A 股特性**：A 股市场和美股有显著差异，经典因子的表现也不同

---

## 1. 单因子研究框架

### 1.1 标准研究流程

```
原始数据                因子计算              因子预处理
daily/daily_basic ──► 计算因子值 ──► 去极值→标准化→中性化
       │                                      │
       ▼                                      ▼
  未来收益率            IC 分析              分组回测
  (T+1~T+20)  ◄──── Rank IC/ICIR ◄──── 5组分层回测
                         │                    │
                         ▼                    ▼
                    IC 衰减分析          多空净值曲线
                    IC 分布特征          分组年化收益
                    胜率统计              最大回撤
                                          换手率
```

### 1.2 核心代码框架

```python
import pandas as pd
import numpy as np
from scipy import stats
from quantdb.api.data_api import DataAPI

api = DataAPI()

# ============================================================
# 第一步：加载数据
# ============================================================

# 获取沪深300成分股（或全 A 股）
# 建议先用沪深300做快速验证，再扩展到全 A
universe = api.get_stock_list()  # 全量股票
trade_dates = api.get_trade_calendar("20150101", "20251231")

# 加载日线行情 + 每日指标
df_daily = api.get_daily(universe, "20150101", "20251231")
df_basic = api.get_daily_basic(universe, "20150101", "20251231")

# ============================================================
# 第二步：计算因子值
# ============================================================

def calc_factor(df, factor_name):
    """
    因子计算入口，返回 DataFrame[ts_code, trade_date, factor_value]
    """
    if factor_name == "momentum_20d":
        # 20日动量 = 当日收盘价 / 20日前收盘价 - 1
        factor = df.groupby("ts_code")["close"].pct_change(20)
    elif factor_name == "size":
        # 对数市值
        factor = np.log(df["total_mv"])
    elif factor_name == "value_ep":
        # EP = 1 / PE_TTM（市盈率倒数）
        factor = 1.0 / df["pe_ttm"]
    # ... 其他因子
    return factor

# ============================================================
# 第三步：因子预处理
# ============================================================

def preprocess_factor(df, factor_col, industry_col=None):
    """
    因子预处理三部曲：去极值 → 标准化 → （可选）行业中性化
    """
    result = df.copy()
    
    # 3.1 去极值（MAD 方法，比 Winsorize 更稳健）
    median = result[factor_col].median()
    mad = (result[factor_col] - median).abs().median()
    upper = median + 5 * 1.4826 * mad
    lower = median - 5 * 1.4826 * mad
    result[factor_col] = result[factor_col].clip(lower, upper)
    
    # 3.2 Z-Score 标准化
    mean = result[factor_col].mean()
    std = result[factor_col].std()
    if std > 0:
        result[factor_col] = (result[factor_col] - mean) / std
    
    # 3.3 行业中性化（可选）
    if industry_col:
        industry_mean = result.groupby(industry_col)[factor_col].transform("mean")
        result[factor_col] = result[factor_col] - industry_mean
    
    return result

# ============================================================
# 第四步：IC 分析
# ============================================================

def calc_ic_series(factor_df, return_df, method="spearman"):
    """
    计算每期的截面 IC（因子值与未来收益的相关系数）
    
    参数：
        factor_df: 因子值面板 (index=trade_date, columns=ts_code)
        return_df: 未来收益面板（同结构）
        method: "spearman"(Rank IC) 或 "pearson"(IC)
    
    返回：IC 时间序列
    """
    ic_series = {}
    for date in factor_df.index:
        f = factor_df.loc[date].dropna()
        r = return_df.loc[date].dropna()
        common = f.index.intersection(r.index)
        if len(common) < 30:  # 样本太少，跳过
            continue
        if method == "spearman":
            ic, _ = stats.spearmanr(f[common], r[common])
        else:
            ic, _ = stats.pearsonr(f[common], r[common])
        ic_series[date] = ic
    
    return pd.Series(ic_series)

def ic_analysis(ic_series):
    """IC 统计分析"""
    result = {
        "IC_Mean": ic_series.mean(),
        "IC_Std": ic_series.std(),
        "ICIR": ic_series.mean() / ic_series.std() if ic_series.std() > 0 else 0,
        "IC_Skew": ic_series.skew(),
        "IC_Positive_Ratio": (ic_series > 0).mean(),  # 胜率
        "IC_Abs_GT_002": (ic_series.abs() > 0.02).mean(),  # 显著比例
        "IC_T_stat": stats.ttest_1samp(ic_series, 0).statistic,
        "IC_P_value": stats.ttest_1samp(ic_series, 0).pvalue,
    }
    return result

# ============================================================
# 第五步：分组回测
# ============================================================

def group_backtest(factor_df, return_df, n_groups=5):
    """
    分层回测：按因子值将股票分成 N 组，计算每组收益
    
    返回：各组的累计收益 DataFrame
    """
    group_returns = {g: [] for g in range(1, n_groups + 1)}
    
    for date in factor_df.index:
        f = factor_df.loc[date].dropna()
        r = return_df.loc[date].dropna()
        common = f.index.intersection(r.index)
        
        if len(common) < n_groups * 10:
            continue
        
        # 按因子值分组
        f_sorted = f[common].rank(pct=True)
        for g in range(1, n_groups + 1):
            lower = (g - 1) / n_groups
            upper = g / n_groups
            mask = (f_sorted > lower) & (f_sorted <= upper)
            if mask.sum() > 0:
                group_returns[g].append(r[common][mask].mean())
            else:
                group_returns[g].append(0)
    
    result = pd.DataFrame(group_returns, index=factor_df.index[:len(group_returns[1])])
    return result
```

---

## 2. 六大经典因子详解

### 2.1 市值因子（Size Factor）— 小盘效应

> **学术背景**：Fama-French 三因子模型中的 SMB（Small Minus Big）。在 A 股，小盘股长期跑赢大盘股（但 2017 年后显著衰减）。

```python
# 因子定义
factor_size = np.log(df_basic["total_mv"])  # 对数总市值

# 预期方向：负向（小市值 → 高收益）
# 因此做 IC 时，IC < 0 表示小盘效应成立

# A 股特殊注意事项：
# 1. 2013-2015 小盘暴涨（创业板牛市），小盘效应极强
# 2. 2017-2020 核心资产牛市，大盘股反而领先
# 3. 2021 至今，小盘再度占优
# → 结论：市值因子在 A 股有很强的「牛熊切换」特征
```

**研究要点**：
| 分析维度 | 具体内容 |
|---------|---------|
| 分组 | 按市值五分位，看各组年化收益差异 |
| 时间稳定性 | 按年度滚动计算 IC，观察方向是否稳定 |
| 板块差异 | 主板 vs 创业板 vs 科创板的市值效应差异 |
| 非线性 | 小盘效应是否集中在最小的 10% 股票（壳价值效应）|
| 交互 | 小盘+低PE 是否比单纯小盘更好 |

**预期结果**（A 股历史经验）：
- 全区间 Rank IC ≈ -0.03 ~ -0.05
- ICIR ≈ -0.3 ~ -0.5
- 多空（小-大）年化 ≈ 5% ~ 15%（区间依赖性强）
- **注意**：2017-2020 区间 IC 可能翻正

---

### 2.2 价值因子（Value Factor）— 低估值溢价

> **学术背景**：Fama-French 的 HML（High B/M Minus Low B/M）。低估值股票长期跑赢高估值。

```python
# EP 因子（市盈率倒数，越高越便宜）
factor_ep = 1.0 / df_basic["pe_ttm"]
# 处理负PE（亏损股）：可以直接剔除，或设为 0
factor_ep = factor_ep.where(df_basic["pe_ttm"] > 0, np.nan)

# BP 因子（市净率倒数）
factor_bp = 1.0 / df_basic["pb"]
factor_bp = factor_bp.where(df_basic["pb"] > 0, np.nan)

# 预期方向：正向（高EP/BP → 高收益）

# A 股特殊注意事项：
# 1. 银行股 PE 常年最低，但未必持续跑赢
# 2. 需要做行业中性化，否则实际上是在做行业轮动
# 3. A 股估值因子在 2019-2020 严重失效（核心资产泡沫期）
# 4. PE_TTM 比 PE 更好，因为避免了季报时点差异
```

**研究要点**：
| 分析维度 | 具体内容 |
|---------|---------|
| 行业中性 | 中性化前后的 IC 对比，确认是否被行业效应驱动 |
| 亏损股 | 亏损股（PE<0）的处理方式对结果的影响 |
| 复合估值 | EP + BP + SP（销售市值比）等权复合 |
| 估值陷阱 | 低估值+业绩下滑 = 价值陷阱，如何识别？ |

---

### 2.3 动量因子（Momentum Factor）— 趋势延续

> **学术背景**：Jegadeesh & Titman (1993)。过去表现好的股票未来继续表现好。但 A 股动量因子和美股有显著差异！

```python
# 20 日动量
factor_mom_20d = df.groupby("ts_code")["close"].pct_change(20)

# 60 日动量
factor_mom_60d = df.groupby("ts_code")["close"].pct_change(60)

# 跳过最近 1 天（避免微观结构噪音）
# close(T) / close(T-21) - 1
factor_mom_20d_skip1 = (
    df.groupby("ts_code")["close"].shift(1) / 
    df.groupby("ts_code")["close"].shift(21) - 1
)

# ⚠️ A 股动量因子的核心发现：
# 1. 短期（1-5日）：反转效应显著（IC < 0）
# 2. 中期（20-60日）：动量效应偶尔有效，但不稳定
# 3. 长期（120-250日）：反转效应再次出现
# → A 股是「反转市」而非「动量市」！
# → 这是 A 股和美股最大的差异之一
```

**研究要点**：
| 分析维度 | 具体内容 |
|---------|---------|
| 多窗口 | 5d/10d/20d/60d/120d/250d 动量的 IC 对比 |
| 反转发现 | 找到动量→反转的切换窗口（通常 A 股在 20-40 天） |
| 涨跌停影响 | 涨停（动量极端值）后的收益分布 |
| 牛熊差异 | 牛市动量有效、熊市反转有效？ |
| 量价交互 | 放量上涨 vs 缩量上涨的后续差异 |

**预期结果（A 股）**：
- 5 日反转 IC ≈ -0.02 ~ -0.04（反转有效）
- 20 日动量 IC ≈ -0.01 ~ 0.01（方向不确定）
- **关键发现**：A 股的散户主导结构导致「过度反应→均值回归」是主旋律

---

### 2.4 换手率因子（Turnover Factor）— 流动性溢价

> **学术背景**：Amihud (2002)。低流动性股票有溢价。在 A 股效果非常好。

```python
# 日均换手率（过去20日均值）
factor_turnover = df_basic.groupby("ts_code")["turnover_rate"].transform(
    lambda x: x.rolling(20).mean()
)

# 方向：负向（低换手 → 高收益）
# 即：IC < 0 为有效

# A 股换手率因子的特殊优势：
# 1. A 股散户占比高，换手率整体远高于成熟市场
# 2. 低换手率 ≈ 机构持仓为主 ≈ 更理性定价
# 3. 换手率因子在 A 股的 ICIR 通常是所有单因子中最高的之一
# 4. 但注意：低换手 + 小盘 = 流动性风险，实盘冲击成本大
```

**研究要点**：
| 分析维度 | 具体内容 |
|---------|---------|
| 换手率区间 | 不同换手率水平（<1%、1-3%、3-5%、>5%）的收益特征 |
| 与市值交互 | 控制市值后，换手率因子是否仍有效 |
| 异常换手 | 突然放量（换手率突增）后的收益 |
| 换手率变化 | 换手率变化率 vs 换手率绝对值的 IC 对比 |

---

### 2.5 波动率因子（Volatility Factor）— 低波动异象

> **学术背景**：Ang et al. (2006)。低波动率股票反而收益更高（违反 CAPM 直觉）。

```python
# 20日收益率标准差
daily_return = df.groupby("ts_code")["close"].pct_change(1)
factor_vol_20d = daily_return.groupby("ts_code").rolling(20).std().reset_index(level=0, drop=True)

# 方向：负向（低波动 → 高收益）

# 延伸因子：
# 1. 下行波动率（Downside Vol）：只计算负收益日的标准差
# 2. 特质波动率（IVOL）：对 Fama-French 三因子回归后的残差波动率
# 3. 最大日涨幅（MAX）：过去20日最大单日涨幅（也是负向因子）
```

---

### 2.6 质量因子（Quality Factor）— ROE / 盈利能力

> **学术背景**：Novy-Marx (2013)。高盈利能力的公司收益更好。

```python
# ⚠️ PIT 警告：财务数据必须用 ann_date 过滤！

# 读取财务指标（PIT 模式）
df_fina = api.get_financials_pit(
    universe,
    fields=["roe", "gross_margin", "debt_to_assets"],
    as_of_date="20240101"
)

# ROE 因子
factor_roe = df_fina["roe"]

# 毛利率因子
factor_gm = df_fina["gross_margin"]

# 杠杆因子（低杠杆 → 高收益）
factor_leverage = -1 * df_fina["debt_to_assets"]

# A 股 ROE 因子特点：
# 1. ROE 在 A 股有效但 IC 不高（~0.02），不如市值和换手率
# 2. ROE 的变化率（ROE 改善）比 ROE 绝对值更有效
# 3. 扣非 ROE（roe_dt）比普通 ROE 更好
# 4. 季度更新频率低，信号更新慢
```

---

## 3. 单因子评估报告模板

每个因子完成研究后，应产出以下内容：

### 3.1 评估报告结构

```markdown
# [因子名称] 研究报告

## 1. 因子定义
- 表达式：...
- 数据来源：...
- 方向：正/负向
- 更新频率：日/月/季度

## 2. IC 分析
- Rank IC 均值：
- ICIR：
- IC 胜率：
- t 统计量 / p 值：
- IC 时序图（按月/按年）

## 3. 分组回测
- 5 组年化收益（等权）
- 多空年化收益：
- 多空夏普比率：
- 多空最大回撤：
- 各组净值曲线图

## 4. 稳健性检验
- 分年度 IC 表（是否逐年衰减）
- 牛市 vs 熊市 IC
- 大盘 vs 小盘 IC
- 行业中性化前后 IC

## 5. 结论与建议
- 因子是否有效？有效区间？
- 是否受市值/行业驱动？
- 实盘可行性（换手率、冲击成本）？
- 是否纳入多因子模型？
```

### 3.2 关键图表

| 图表 | 说明 | 关注点 |
|------|------|--------|
| IC 时序图 | IC 值随时间变化的折线图 | 是否有方向漂移？在某段时间完全失效？ |
| IC 分布直方图 | IC 值的频率分布 | 是否接近正态？偏度如何？ |
| 分组累计收益图 | 5 组净值曲线 | 是否单调递增？哪组最突出？ |
| 因子值分布图 | 每期因子值的分布 | 是否有极端值？是否需要截尾？ |
| IC 衰减曲线 | T+1 到 T+60 的 IC 变化 | 因子有效期多长？决定调仓频率 |
| 月度热力图 | 每月 IC 的热力图 | 是否有日历效应（如年报季前后）|

---

## 4. 阶段一的成功标准

完成阶段一后，你应该：

- [ ] 能够流畅地使用 QuantDB 的 DataAPI 读取各类数据
- [ ] 建立了标准化的单因子研究 Pipeline（从计算到评估的完整流程）
- [ ] 完成了 6 个经典因子的研究报告
- [ ] 理解 A 股因子的特殊性（反转>动量、小盘效应的周期性等）
- [ ] 掌握了 PIT 模式和前视偏差的实践处理
- [ ] 有了一个能复用的因子评估代码库

---

## 5. 常见坑与排雷

| 问题 | 症状 | 解决方案 |
|------|------|---------|
| 复权问题 | 因子值在分红日突变 | 确认使用前复权价格，或使用 adj_factor 手动处理 |
| 停牌股 | 收益率为 0，拉低 IC | 用 is_suspended 字段过滤停牌日 |
| 新股 | 上市首日涨幅极端 | 剔除上市不足 60 天的股票 |
| ST 股 | 估值异常 | 默认剔除 ST 股票 |
| 涨跌停 | 因子打分高但买不进 | 在调仓日检查 is_limit_up |
| 行业偏差 | 因子选出一堆银行股 | 做行业中性化处理 |
| 回看偏差 | 用全区间选因子参数 | 划分样本内/外 |

---

> 完成本阶段后，进入 **02-多因子模型框架.md**
