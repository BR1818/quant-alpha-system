# 量化Alpha系统 — 终极审查报告

> 审查日期：2026-05-29（综合两轮独立审计）
> 审查角色：资深量化系统架构师 × A股量化研究员 × Python后端工程师 × A股实战大作手
> 审查范围：全量代码（core / modules / scripts / tests / config）

---

## 一、总体评估

当前系统**骨架和架构是优秀的**——`core→modules→scripts`三层分离、Protocol接口化设计、FactorRegistry注册器模式、PipelineEngine编排追踪，具备了一个量化系统的标准脚手架。

但在**量化业务核心逻辑层面存在多处致命缺陷**：预测模块是"伪装成AI的规则引擎"、未来函数/前视偏差未根除、回测不符合A股T+1规则、因子预处理缺失去极值和中性化、LSTM/XGBoost训练流程严重失当。整体风险等级：**高风险**。

**当前回测结果完全不可信，不可用于实盘决策。** 但只要按本报告的路线图逐步修复，这套系统完全有潜力成为一个年化15-30%的私人量化交易平台。

### 综合评分

| 维度 | 得分 (10分制) | 一句话点评 |
|------|:---:|------|
| **架构设计** | **7.5** | 分层合理、接口标准，Pipeline引擎有前瞻性，但有接口/实现混放、无DAG调度 |
| **因子体系** | **3.5** | 数量不足（26个中仅12个稳定可用）、公式有误（RSI/ATR）、缺去极值/中性化 |
| **预测模型** | **2.0** | run_analysis实际运行手写规则非LSTM；LSTM无标准化/早停；XGBoost全量训练 |
| **回测框架** | **4.0** | 无T+1限制、佣金严重偏低、无涨跌停/停牌处理、缺净值曲线 |
| **数据层** | **4.5** | 无复权、财报对齐有未来函数、无停牌/ST/退市过滤、缓存无淘汰 |
| **代码质量** | **6.0** | 异常处理尚可，但缺lint/类型检查、测试引用不存在的模块、硬编码严重 |
| **风控体系** | **2.0** | 几乎为零——无动态止损、无大盘择时、无异常信号拦截、无熔断机制 |

---

## 二、致命问题清单（共14项）

### 🚨 Tier 1：量化致命问题（回测结果完全不可信）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| **C1** | **"伪预测"——run_analysis.py的predict_step是手写规则引擎** | `run_analysis.py:59-145` | 输出"上涨概率65%"等数字给用户AI预测错觉，实际只是 `50 + 动量×200` 线性公式，与LSTM/XGBoost无关 |
| **C2** | **fina_indicator财报对齐存在未来函数** | `quantdb_loader.py:88-96` | ann_date落在非交易日时merge on trade_date丢弃财报数据，应使用`merge_asof` |
| **C3** | **无复权处理** | `quantdb_loader.py:39-48` | Tushare daily返回不复权数据，除权日价格跳空导致技术因子和回测全面失真 |
| **C4** | **无停牌/退市/ST过滤** | `quantdb_loader.py` 全文 | 回测可在不可交易日成交、选股选中ST/退市股 |
| **C5** | **回测无T+1限制** | `bt_engine.py:64-99` | A股当日买入当日可卖出，回测严重虚高 |
| **C6** | **佣金严重偏低且无印花税** | `bt_engine.py:110` | 代码万3双向，A股实际买卖综合约0.152%，低估5倍 |
| **C7** | **XGBoost全量训练无时序切分** | `xgboost_selector.py:44-45` | 模型训练时已"看到"测试期数据，选股结果完全不可信 |
| **C8** | **LSTM无数据标准化+无验证集/早停+MSELoss多任务量纲不匹配** | `lstm_predictor.py` 全文 | 预测结果完全不可信：无标准化导致梯度爆炸、无早停必然过拟合、多任务损失被大数值主导 |
| **C9** | **因子预处理缺失MAD去极值和行业/市值中性化** | `preprocessor.py` | A股因子截面不可比，选股排序失真 |

### ⚠️ Tier 2：严重问题（核心功能缺失或逻辑错误）

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| **C10** | **Ensemble选股分数量纲不一致直接加权** | `ensemble_selector.py:73` | XGB概率[0,1]与Composite排名分范围不同，`0.7*xgb + 0.3*comp`无数学意义 |
| **C11** | **RSI使用SMA而非Wilder EMA** | `technical.py:75-76` | 与经典定义不一致，趋势行情中SMA版RSI更滞后 |
| **C12** | **量比rolling均值包含当日数据（未来函数）** | `moneyflow.py:46`, `extended.py:109` | 量比分母应排除当日volume |
| **C13** | **PE_TTM/PB负值取倒数语义错误** | `fundamental.py:14-15,30` | 亏损公司/资不抵债公司与正常公司被"镜像对称"处理 |
| **C14** | **全局中位数填充NaN是未来函数** | `preprocessor.py:32-36` | 回测时刻T不应偷看全序列中位数 |

---

## 三、分维度详细审查

### 3.1 系统架构层面

#### 3.1.1 架构优点 ✅

| 维度 | 评价 |
|------|------|
| **分层清晰** | `core→modules→scripts` 三层分离，职责边界明确 |
| **协议化设计** | `Factor` / `DataLoader` / `StockSelector` 等使用 Protocol 接口，新增模块只需实现标准方法 |
| **注册器模式** | `FactorRegistry` 集中管理，热插拔式因子扩展 |
| **管道引擎** | `PipelineEngine` 支持步骤编排、异常隔离、执行追踪 |
| **Point-in-Time意识** | `quantdb_loader.py` 对 `fina_indicator` 使用 `ann_date`（披露日）而非 `end_date`（截止日），方向正确（但实现有缺陷，见C2） |
| **双层缓存** | 内存+磁盘缓存（`DataCache`），避免重复读取Parquet |

#### 3.1.2 架构缺陷

**问题1：FactorRegistry放在interfaces模块中，违反接口/实现分离**

`core/interfaces/factor.py`中`FactorRegistry`是具体实现类，不应与`Factor` Protocol同处接口层。

> 修复：将`FactorRegistry`移至`core/registry.py`或独立的`core/factor_registry.py`。

**问题2：Pipeline Engine无DAG调度、无容错、无超时**

`engine.py`只是简单的for循环，任一步骤异常即终止整个pipeline。无步骤依赖声明（无法并行计算独立因子）、无`continue_on_error`选项、无超时控制。

> 修复：增加`depends_on`参数构建DAG，独立步骤并行执行；增加`timeout`/`max_retries`/`fallback`机制。

**问题3：Context线程不安全+内存无限增长**

`context.py`中`traces: List`、`intermediate_results: Dict`全是裸可变容器，无线程锁保护。`intermediate_results`无容量限制，批量回测5000只股票时会OOM。Logger FileHandler创建后不关闭，批量回测耗尽文件描述符。

> 修复：增加LRU淘汰+`max_size`限制；`traces`增加环形缓冲；加`threading.Lock`；实现`close()`方法关闭handler。

**问题4：Protocol接口缺少关键方法**

- `Factor` Protocol缺少`get_dependencies()`和`lookback_period`——引擎无法解析因子依赖和确定计算顺序
- `StockPredictor` Protocol缺少`save()`/`load()`持久化接口——训练好的模型无法复用
- `StockSelector` Protocol缺少仓位分配、风控参数接口
- 缺少`BacktestEngine`、`RiskManager`、`PortfolioOptimizer`等核心Protocol

**问题5：异常体系不完善**

- `core/exceptions.py`中所有异常类都是空壳`pass`，无上下文信息
- 缺少`StrategyError`、`RegistryError`、`ConfigError`
- `PipelineRegistry`使用`KeyError`而非自定义异常

**问题6：扩展性评估**

| 新增场景 | 当前改造成本 | 问题 |
|---------|------------|------|
| 新增因子 | 中等 | 需手动调用`register()`，无装饰器自动注册 |
| 新增策略 | 高 | 无`StrategyRegistry`，需手动管理实例 |
| 新增数据源 | 高 | 无`DataLoaderRegistry`，需改所有调用方 |
| 新增API接口 | 极高 | 无接口层，直接暴露脚本 |

---

### 3.2 量化业务核心逻辑（重中之重）

#### 3.2.1 "伪预测"问题（C1详述）

**这是两轮审计中一致认为最严重的问题。** `run_analysis.py`第59-145行的`predict_step`并没有使用任何机器学习模型：

```python
# 实际上只是一段手写的均线判断规则
momentum = (close[-1] / close[-20]) - 1
base_prob = 50.0 + (momentum * 200)  # 用动量线性映射到概率
```

缺陷清单：

| 缺陷 | 说明 |
|------|------|
| **误导性** | 输出"上涨概率 65%"之类的数字，给用户AI预测的错觉 |
| **无衰减建模** | 使用`0.85^i`固定指数衰减模拟"越远越不确定"，是假设不是预测 |
| **胜率是伪造的** | `win_rate = base_prob * 1.05`，直接从概率转换，非历史验证 |
| **无训练过程** | LSTM和XGBoost的`.train()`从未被任何脚本调用 |

> 修复：删除伪预测硬编码，实现 `create_sequences() → LSTMPredictor.train() → predict()` 的完整链路。

#### 3.2.2 未来函数排查（全量扫描）

| 位置 | 操作 | 风险 | 结论 |
|------|------|------|------|
| `quantdb_loader.py:88-96` | fina_indicator按trade_date merge | **致命** | ann_date落在非交易日时财报被丢弃，应使用`merge_asof` |
| `quantdb_loader.py:39-48` | daily表不复权 | **致命** | 除权日价格跳空导致因子和回测失真 |
| `quantdb_loader.py` | 无停牌/退市/ST过滤 | **致命** | 回测可在不可交易日成交 |
| `preprocessor.py:32-36` | 全局中位数填充NaN | **严重** | 回测时刻T不应偷看全序列 |
| `preprocessor.py:51-55` | transform()用新数据中位数填充 | **中等** | 应使用训练集保存的中位数 |
| `moneyflow.py:46` | `rolling(5).mean()`含当日 | **中等** | 量比分母应排除当日volume |
| `extended.py:109` | `rolling(20).mean()`含当日 | **中等** | 同上 |
| `xgboost_selector.py:44-45` | 全量训练无时序切分 | **致命** | 模型在训练时已"看到"测试期数据 |
| `labels/__init__.py:50-55` | create_sequences未检查特征NaN | **中等** | 停牌期数据被跨过，样本不连续 |
| 所有rolling因子 | `min_periods=1`默认 | **轻微** | 新股上市初期因子值不可靠 |

其余shift/rolling/pct_change操作（MA/MACD/Bollinger/Return/ATR/KDJ/CCI/Williams%R/OBV/Amihud/PriceVolumeCorr/MarginBalance）方向均为向后看，**无未来函数**。

#### 3.2.3 回测逻辑

| 问题 | 位置 | 说明 |
|------|------|------|
| **无T+1限制** | `bt_engine.py:64-99` | A股当日买入不可卖出，代码无检查 |
| **buy_price记录收盘价非成交价** | `bt_engine.py:69` | 止损止盈基于错误价格判断 |
| **止损用收盘价非盘中低点** | `bt_engine.py:77` | 低估止损频率和亏损幅度 |
| **下单数量用收盘价计算** | `bt_engine.py:66` | 实际成交用次日开盘价，跳空高开时可用现金不足 |
| **涨跌停未处理** | `bt_engine.py` | 可在涨停价买入、跌停价卖出 |
| **停牌日未过滤** | `bt_engine.py` | 停牌日仍可虚拟成交 |
| **仅支持单股回测** | `run_backtest.py` | 无法做组合级别回测 |
| **CompositeSelector被误用于时间序列** | 回测流程 | 本为截面选股设计，在单股时序上做rank无统计意义 |

```python
# T+1修复
self.buy_bar = None
# 买入时
self.buy_bar = len(self)
# 卖出判断前
if self.buy_bar and len(self) - self.buy_bar < 2:
    return  # 买入后不足1个完整交易日，不可卖出

# notify_order中记录实际成交价
def notify_order(self, order):
    if order.status == order.Completed and order.isbuy():
        self.buy_price = order.executed.price
        self.buy_bar = len(self)
```

#### 3.2.4 交易成本

当前：`setcommission(commission=0.0003)` — 万3双向，**远低于A股实际**。

| 费用项 | A股实际 | 代码实现 |
|--------|--------|---------|
| 佣金 | 万2.5双向 + 最低5元 | 万3双向，无最低 |
| 印花税 | 千1卖出单向 | 无 |
| 过户费 | 十万分之一双向 | 无 |
| **综合单次往返** | **~0.152%** | **0.03%（低估5倍）** |

```python
# 修复：自定义A股佣金类
class ACStockCommission(bt.CommInfoBase):
    params = (
        ('commission', 0.00025),
        ('stamp_duty', 0.001),
        ('min_commission', 5.0),
    )
    def _getcommission(self, size, price, pseudoexec):
        comm = max(abs(size) * price * self.p.commission, self.p.min_commission)
        if size < 0:  # 卖出
            comm += abs(size) * price * self.p.stamp_duty
        return comm
```

#### 3.2.5 因子计算

**公式错误类：**

| 因子 | 问题 | 位置 | 修复 |
|------|------|------|------|
| RSI | 使用SMA而非Wilder EMA | `technical.py:75-76` | `gain.ewm(alpha=1/period, adjust=False).mean()` |
| RSI | 连续上涨时avg_loss=0返回NaN | `technical.py:77` | `np.where(avg_loss==0, 100.0, 100-100/(1+rs))` |
| ATR | 使用SMA而非Wilder EMA | `technical.py:127` | `tr.ewm(alpha=1/period, adjust=False).mean()` |
| PE_TTM | 负PE取倒数符号翻转 | `fundamental.py:14-15` | `ep[pe < 0] = np.nan` |
| PB | 负PB同样问题 | `fundamental.py:30` | `pb[pb < 0] = np.nan` |

**因子设计缺陷：**

| 问题 | 说明 |
|------|------|
| MACD未归一化 | 高价股MACD天然大于低价股，横向无意义 |
| ATR未归一化 | 高价股ATR天然高，建议输出ATR/close |
| OBV用pct_change标准化 | OBV可穿过0轴，pct_change产生Inf，应改用diff |
| 布林带名称与含义不匹配 | name="bollinger"但计算的是%BW(带宽)，不是%B |
| KDJ只输出K值 | 缺D值和J值，J=3K-2D更有Alpha价值 |
| 年化波动率系数 | `np.sqrt(252)`应为`np.sqrt(242)`（A股实际交易日） |

**因子覆盖问题：**

| 类别 | 稳定可用 | 经常缺失 | 完全不可用 |
|------|---------|---------|-----------|
| 技术(5) | 5 | 0 | 0 |
| 基本面(7) | 6 | 1(net_profit_yoy) | 0 |
| 资金流(4) | 2 | 0 | 2(northbound_flow, top_list_net_buy) |
| 情绪(3) | 0 | 1(limit_up_count) | 2(top_list_net_buy, margin_balance) |
| 扩展(14) | 14 | 0 | 0 |
| **合计(33)** | **27** | **2** | **4** |

**核心缺失**：大盘择时因子、板块动量因子、地量信号、融资买入占比等A股核心Alpha来源。

**因子预处理缺失：**

A股因子处理标准流程：**去极值(MAD) → 填充缺失 → 标准化 → 行业+市值中性化**

当前仅做了标准化（且顺序错误：先填充再标准化），缺失步骤1和步骤4。

```python
# MAD去极值
median = np.nanmedian(X, axis=0)
mad = np.nanmedian(np.abs(X - median), axis=0)
upper = median + 3 * 1.4826 * mad
lower = median - 3 * 1.4826 * mad
X = np.clip(X, lower, upper)
```

#### 3.2.6 LSTM训练流程

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 无数据标准化 | **致命** | 金融数据量纲差异巨大，不标准化导致梯度爆炸/消失 |
| 无验证集/早停 | **致命** | 100 epoch全量训练，必然过拟合 |
| MSELoss多任务量纲不匹配 | **致命** | daily_prob[0,1]、trend、target_price(绝对价格)量纲不同，大数值主导损失 |
| 输出未做sigmoid激活 | **严重** | "daily_prob"可以是任意实数，非概率 |
| 全批量训练 | **中等** | 无DataLoader+mini-batch，效率低且易局部最优 |
| 模型缺正则化 | **中等** | dropout=0.2偏低，FC前无dropout，无LayerNorm |

```python
# 修复要点
# 1. 训练前标准化
self.scaler = StandardScaler()
X_2d = self.scaler.fit_transform(X.reshape(-1, n_features))

# 2. 验证集+早停
split = int(len(X) * 0.8)
X_train, X_val = X[:split], X[split:]
# patience=10 early stopping

# 3. 多任务加权损失
class MultiTaskLoss(nn.Module):
    def forward(self, pred, target):
        return (self.bce(pred[:, 0], target[:, 0]) +
                0.5 * self.mse(pred[:, 1], target[:, 1]) +
                0.5 * self.huber(pred[:, 2], target[:, 2]))

# 4. 概率输出加sigmoid
result = {"daily_prob": torch.sigmoid(predictions[:, 0]), ...}
```

#### 3.2.7 XGBoost选股器

| 问题 | 严重度 | 说明 |
|------|--------|------|
| 全量训练无时序切分 | **致命** | 信息泄露，回测失真 |
| fillna(0)策略危险 | **严重** | 0可能有实际含义，应使用截面中位数 |
| 无正则化参数 | **中等** | 无gamma/min_child_weight/reg_lambda，易过拟合 |
| pickle反序列化安全隐患 | **中等** | 应改用XGBoost原生JSON格式 |
| 从未被任何脚本调用 | **严重** | 死代码，需接入完整训练流水线 |

#### 3.2.8 集成选股分数量纲不一致

`ensemble_selector.py:73`：XGB输出概率[0,1]，Composite输出截面排名加权分（范围不定），直接加权无数学意义。LSTM分数始终为0（空置）。

```python
# 修复：融合前标准化到统一尺度
def _normalize_score(s: pd.Series) -> pd.Series:
    return (s - s.min()) / (s.max() - s.min() + 1e-8)

xgb_norm = _normalize_score(xgb_scores)
comp_norm = _normalize_score(comp_scores)
final_scores = 0.7 * xgb_norm + 0.3 * comp_norm
```

#### 3.2.9 三分类标签NaN处理

`labels/__init__.py:32`：`pd.Series(1, index=data.index)`初始化默认值为1（震荡），NaN行也被标记为"震荡"而非NaN。

```python
# 修复
labels = pd.Series(np.nan, index=data.index)
labels[fwd > up_threshold] = 2
labels[fwd < down_threshold] = 0
labels[(fwd >= down_threshold) & (fwd <= up_threshold)] = 1
```

#### 3.2.10 从大作手视角看策略缺陷

1. **忽视大盘择时**：A股"大盘跌，个股不生"是铁律。当沪指处于250日均线下方时，即使个股评分再高也大概率亏钱。当前完全无大盘过滤器。

2. **忽视板块联动**：涨停板的80%来自板块效应而非个股基本面。虽有板块选股，但未分析板块动量（是否正在走强），未做板块轮动策略。

3. **忽视量价结构**："量在价先"——放量突破和缩量回调是A股最核心的交易信号。当前仅一个`volume_ratio`因子，缺少地量信号、突破均量线的放量确认等关键量价因子。

4. **缺少动态风控**：止盈止损写死百分比，无ATR动态止损、无保本止损、无系统性风险强制减仓机制。

---

### 3.3 代码工程质量

#### 编码规范

- 全项目无lint配置（无ruff/flake8/mypy），代码质量全靠人工

#### 硬编码问题

| 硬编码项 | 位置 | 应改为 |
|---------|------|--------|
| 默认股票代码`600584.SH` | 5个脚本 | 从配置读取或设为required |
| LSTM超参数 | `run_analysis.py:113-115` | 从settings.yaml读取 |
| XGBoost无正则化参数 | `xgboost_selector.py:21-30` | 增加gamma/reg_lambda等 |
| 板块扫描上限100只 | `run_sector_scan.py:61` | 参数化 |
| sigmoid魔数`*20`/`[15,85]`/`0.9^i`/`0.3*i` | `run_analysis.py:137-145` | 配置化或删除 |
| Fallback股票池仅5只 | `train_xgboost.py:49` | 抛出异常或明确警告 |
| end_date默认`20251231`（已过期） | `train_xgboost.py:27` | 用`datetime.now()` |

#### 致命运行时错误

| 错误 | 位置 | 说明 |
|------|------|------|
| `from backtest.metrics import calculate_metrics` | `test_backtest.py:9` | 引用不存在的模块，测试完全无法运行 |
| `registry`变量未定义 | `run_sector_scan.py:43` | `NameError`必现 |
| volume列名不一致 | `bt_engine.py:128` vs `html_reporter.py:62` | `vol` vs `volume`，图表无法渲染 |
| 净值曲线数据源缺失 | `html_reporter.py:101` | `portfolio_values`在BTEngine返回值中不存在 |

#### 测试质量

- `test_backtest.py`引用已删除的模块，无法运行
- `test_data_loader.py`：核心的`load_stock_daily_enriched`合并逻辑完全没有测试
- `test_factors.py`：随机数据无固定seed，不含high/low等因子所需列
- `test_predictor.py`：LSTM测试y的三列无依赖关系，与真实训练流程不匹配
- `test_selector.py`：CompositeSelector测试依赖列名前缀巧合匹配
- 无`conftest.py`，公共fixture无法共享

#### 复用性问题

- 5个脚本中配置加载逻辑完全重复（约10行×5），应提取为`utils.config.load_config()`
- `test_context.py`中5个测试方法4个重复创建config+Context，应用`@pytest.fixture`

---

### 3.4 性能与效率优化

| 问题 | 位置 | 修复 |
|------|------|------|
| CCI逐窗口apply(lambda) | `extended.py:180` | 向量化：`(tp - tp.rolling().mean()).abs().rolling().mean()` |
| Parquet无列裁剪 | `quantdb_loader.py:22-29` | `pq.read_table(file_path, columns=needed)` |
| DataCache无容量限制 | `cache.py` | LRU淘汰+`maxsize`限制 |
| LSTM全批量训练 | `lstm_predictor.py:61-62` | DataLoader+mini-batch |
| FactorEngine全量拷贝 | `engine.py:26` | 只新建因子列，不copy |
| 磁盘缓存用pickle | `cache.py:34,45` | 改用Parquet（安全+兼容） |
| Logger FileHandler泄漏 | `context.py:50` | 实现close()方法关闭handler |

---

### 3.5 数据链路可靠性

| 问题 | 说明 |
|------|------|
| 缺失值处理 | `fillna(0)`在XGBoost中危险——0可能有实际含义 |
| 异常值处理 | 完全未处理极端行情数据 |
| 重复数据处理 | `fina_indicator`中`drop_duplicates(subset=["trade_date"], keep="last")`过于粗暴 |
| 数据版本管理 | 无增量更新机制，无版本标记，无法追溯变更 |
| 数据源容错 | `_load_parquet`返回None但调用方未做None检查 |
| merge无suffixes | 同名列冲突时Pandas自动加`_x`/`_y`后缀 |
| 校验结果被忽略 | `validator.validate()`返回bool但调用方完全忽略 |
| 校验规则缺失A股特有场景 | 无涨跌停/ST/退市/停牌日成交量校验 |

---

### 3.6 稳定性、监控与运维

| 问题 | 说明 |
|------|------|
| 校验失败仅warning不阻断 | `validator.py:46-49`，调用方完全忽略返回值 |
| Pipeline步骤异常即终止 | `engine.py:49-52`，无降级策略 |
| pickle反序列化安全隐患 | `xgboost_selector.py:78-84`，可执行任意代码 |
| data_dir硬编码用户路径 | `settings.yaml:2`，`/Users/zhengborui/...` |
| transaction_cost被代码忽略 | settings.yaml定义了但代码硬编码万3 |
| 脚本用print()不用logger | 日志不写入文件 |
| 板块扫描创建了reporter但从未使用 | `run_sector_scan.py:46` |
| 缺失关键配置项 | 复权方式、停牌策略、ST过滤开关、缓存容量上限、交易日历路径 |

---

### 3.7 风控与安全

**参数风控**：
- 无极值参数拦截（如仓位比例>100%）
- 无异常信号过滤（如全市场涨停时的选股结果）
- 回测无最大回撤止损/熔断机制
- EnsembleSelector硬编码权重无法适应市场变化

**隐私与安全**：
- `pickle.load`反序列化安全隐患，应改用XGBoost原生`save_model`/`load_model`
- 磁盘缓存使用pickle格式，注入恶意pkl可导致RCE
- `settings.yaml`数据路径暴露用户目录结构

---

## 四、代码重构建议

### 4.1 数据层重构（最紧迫）

```python
# quantdb_loader.py — 修复财报对齐 + 增加复权 + 停牌标记
def load_stock_daily_enriched(self, ts_code, start_date, end_date):
    df = self.load_stock_daily(ts_code, start_date, end_date)

    # 前复权处理
    adj = self._load_parquet("adj_factor", ts_code)
    if adj is not None:
        adj["trade_date"] = pd.to_datetime(adj["trade_date"])
        df = df.merge(adj[["trade_date", "adj_factor"]], on="trade_date", how="left")
        for col in ["open", "high", "low", "close"]:
            df[col] = df[col] * df["adj_factor"] / df["adj_factor"].iloc[-1]

    # fina_indicator: 使用 merge_asof 替代 merge on trade_date
    fina = self._load_parquet("fina_indicator", ts_code)
    if fina is not None:
        fina["ann_date"] = pd.to_datetime(fina["ann_date"])
        fina = fina.dropna(subset=["ann_date"]).sort_values("ann_date")
        trade_dates = df["trade_date"].sort_values().values
        fina["effective_date"] = fina["ann_date"].apply(
            lambda d: trade_dates[trade_dates >= d][0]
            if np.any(trade_dates >= d) else pd.NaT
        )
        fina = fina.dropna(subset=["effective_date"])
        df = pd.merge_asof(df, fina[available], left_on="trade_date",
                           right_on="effective_date", direction="backward")
        for c in factor_cols:
            df[c] = df[c].ffill()

    # 停牌日标记
    df["is_tradable"] = df["vol"] > 0
    return df
```

### 4.2 回测引擎重构

```python
# bt_engine.py — T+1限制 + A股佣金 + 涨跌停 + 停牌
class QuantAlphaStrategy(bt.Strategy):
    def __init__(self):
        self.buy_bar = None

    def next(self):
        if self.data.volume[0] == 0:  # 停牌日
            return
        if self.position:
            if self.buy_bar and len(self) - self.buy_bar < 2:  # T+1
                self.hold_days += 1
                return
            if self.data.low[0] <= self.stop_price:  # 盘中止损
                self.order = self.sell(size=self.position.size)
                return
        # ... 买入逻辑

    def notify_order(self, order):
        if order.status == order.Completed and order.isbuy():
            self.buy_price = order.executed.price  # 实际成交价
            self.buy_bar = len(self)
```

### 4.3 预处理器重构

```python
# preprocessor.py — 完整的A股因子预处理流程
class FeaturePreprocessor:
    def fit_transform(self, X, industry=None, market_cap=None):
        # 1. MAD去极值
        median = np.nanmedian(X, axis=0)
        mad = np.nanmedian(np.abs(X - median), axis=0)
        X = np.clip(X, median - 3*1.4826*mad, median + 3*1.4826*mad)
        # 2. 填充缺失（用训练集中位数）
        self._medians = np.nanmedian(X, axis=0)
        for i in range(X.shape[1]):
            X[np.isnan(X[:, i]), i] = self._medians[i]
        # 3. 标准化
        self.scaler = RobustScaler()
        X = self.scaler.fit_transform(X)
        # 4. 行业+市值中性化
        if industry is not None and market_cap is not None:
            X = self._neutralize(X, industry, market_cap)
        return X

    def transform(self, X):
        # 使用训练集参数（_medians, scaler），不偷看新数据
        ...
```

---

## 五、优先级落地清单

### 紧急修复（1-2周，必须马上改）

| # | 修复项 | 文件 | 预估工时 |
|---|-------|------|---------|
| 1 | **删除伪预测，接入真实LSTM预测流水线** | run_analysis.py | 4h |
| 2 | **fina_indicator改用merge_asof对齐ann_date** | quantdb_loader.py | 4h |
| 3 | **增加前复权处理** | quantdb_loader.py | 3h |
| 4 | **回测引擎增加T+1限制** | bt_engine.py | 2h |
| 5 | **修正佣金为A股真实水平** | bt_engine.py | 2h |
| 6 | **buy_price改为notify_order记录实际成交价** | bt_engine.py | 1h |
| 7 | **XGBoost增加按时间切分train/test split** | xgboost_selector.py | 3h |
| 8 | **LSTM增加标准化+验证集+早停+sigmoid激活** | lstm_predictor.py | 6h |
| 9 | **preprocessor增加MAD去极值** | preprocessor.py | 2h |
| 10 | **修复run_sector_scan.py的registry未定义** | run_sector_scan.py | 0.5h |
| 11 | **修复test_backtest.py引用不存在的模块** | test_backtest.py | 2h |
| 12 | **Ensemble选股融合前标准化分数** | ensemble_selector.py | 1h |
| **小计** | | | **30.5h** |

### 中期优化（2-4周，迭代优化）

| # | 优化项 | 文件 | 预估工时 |
|---|-------|------|---------|
| 13 | 增加停牌/ST/退市过滤逻辑 | quantdb_loader.py | 6h |
| 14 | 增加涨跌停成交限制 | bt_engine.py | 3h |
| 15 | 止损判断改用盘中低点 | bt_engine.py | 1h |
| 16 | RSI改用Wilder EMA | technical.py | 1h |
| 17 | ATR改用Wilder EMA | technical.py | 1h |
| 18 | PE_TTM/PB负值处理 | fundamental.py | 1h |
| 19 | 量比rolling排除当日 | moneyflow.py, extended.py | 1h |
| 20 | 增加行业/市值中性化 | preprocessor.py | 6h |
| 21 | 三分类标签NaN行处理 | labels/__init__.py | 0.5h |
| 22 | volume列名统一 | bt_engine.py, html_reporter.py | 1h |
| 23 | LSTM多任务损失函数 | lstm_predictor.py | 2h |
| 24 | XGBoost正则化参数+模型持久化安全化 | xgboost_selector.py | 1h |
| 25 | CCI向量化计算 | extended.py | 0.5h |
| 26 | 配置加载逻辑提取为公共函数 | scripts/ | 1h |
| 27 | DataCache增加LRU淘汰+线程锁 | cache.py | 2h |
| 28 | HTML报告净值曲线数据源修复 | bt_engine.py, html_reporter.py | 3h |
| 29 | 清理废弃backtest/目录 | backtest/ | 0.5h |
| 30 | 补充缺失数据源因子的优雅降级 | moneyflow.py, sentiment.py | 2h |
| **小计** | | | **34.5h** |

### 长期规划（5-6周，架构升级）

| # | 规划项 | 说明 |
|---|-------|------|
| 31 | Pipeline Engine升级为DAG调度 | 支持因子依赖声明、并行计算、容错降级 |
| 32 | 增加RiskManager Protocol | 统一风控：止损/止盈/仓位/回撤/熔断 |
| 33 | 增加大盘择时模块 | 沪深300 MA250过滤，系统性风险空仓 |
| 34 | 增加板块动量因子和轮动策略 | 板块5日涨幅、个股板块内排名 |
| 35 | 增加walk-forward回测框架 | 滚动窗口训练+测试，消除全量训练偏差 |
| 36 | 增加因子IC/IR评估体系 | 因子入库前强制IC>0.03、ICIR>0.5 |
| 37 | 增加基准对比 | 沪深300/中证500，超额收益+信息比率 |
| 38 | 多股组合回测 | 组合级净值曲线、仓位分配 |
| 39 | 增加StrategyRegistry | 对称FactorRegistry设计，装饰器自动注册 |
| 40 | 项目打包为可安装包 | 替换sys.path.insert为pyproject.toml |
| 41 | Web Dashboard | Flask/Streamlit交互式面板 |
| 42 | 定时任务+数据自动更新 | APScheduler + QuantDB增量对接 |

---

## 六、补充专业建议

1. **引入交易日历**：A股交易日与自然日不对齐，所有时间对齐操作必须基于交易日历。建议加载Tushare的`trade_cal`表，在数据层统一维护。

2. **因子有效性检验缺失**：当前没有IC/IR/分层回测等因子评估体系。建议新增`modules/evaluation/`，在因子入库前强制通过IC>0.03、ICIR>0.5的门槛。

3. **Walk-Forward回测框架**：当前回测是单次全区间，建议实现滚动窗口walk-forward：每期用过去N个月训练、未来1个月测试，滚动推进。

4. **数据版本管理**：建议对Parquet数据文件增加版本标记，每次数据更新时保留旧版本，确保回测可复现。

5. **实盘适配层预留**：建议在架构上预留`modules/broker/`接口层，抽象下单、撤单、持仓查询等操作。

6. **监控与告警**：因子覆盖率监控、选股结果分布监控、回测与实盘偏差监控。

7. **配置化一切**：佣金率、滑点、涨跌停比例、ST过滤开关、因子权重、模型参数等全部收敛到`settings.yaml`。

8. **EnsembleSelector动态权重**：基于近期IC动态分配权重，至少支持按市场状态切换权重方案。

9. **回测指标补全**：卡尔玛比率、Sortino比率、月度/年度收益分布、最大连续亏损次数、超额收益、信息比率。

10. **CompositeSelector误用修正**：它本为截面选股设计，在单股时序回测中使用无统计意义。应改为多股截面选股→组合构建→组合回测的正确流程。

---

*审查完成。本文档为两轮独立审计的综合终版。*
